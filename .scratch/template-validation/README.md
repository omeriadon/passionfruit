# Device Template Validation

- `airpods.template.json`: valid against its section schema, zero errors
- `apple-tv.template.json`: valid against its section schema, zero errors
- `apple-watch.template.json`: valid against its section schema, zero errors
- `homepod.template.json`: valid against its section schema, zero errors
- `ipad.template.json`: valid against its section schema, zero errors
- `iphone.template.json`: valid against its section schema, zero errors
- `mac.template.json`: valid against its section schema, zero errors
- `vision.template.json`: valid against its section schema, zero errors
