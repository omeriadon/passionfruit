# Repository instructions

## GitHub and Vercel

- When implementing a requested change, commit the completed atomic change and push it to the intended GitHub branch after validation.
- For Vercel-connected repositories, batch related validated changes before pushing. Each push triggers a Vercel deployment, consumes build time, and may hit deployment limits.
- Do not trigger a production deployment unless the requested work explicitly includes production deployment.
